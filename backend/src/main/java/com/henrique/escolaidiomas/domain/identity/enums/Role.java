package com.henrique.escolaidiomas.domain.identity.enums;

/**
 * Perfis de acesso do sistema (RN-14). Cada subtipo de Usuario tem um Role fixo:
 * Gestao -> GESTAO, Professor -> PROFESSOR, Aluno -> ALUNO.
 */
public enum Role {
    GESTAO,
    PROFESSOR,
    ALUNO
}
