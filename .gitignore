# --- Base do projeto: não versionar ---

# Documentação interna (não sobe pro GitHub)
Docs/

# Configuração do Claude Code (skills, hooks, settings)
.claude/

# Segredos e variáveis de ambiente
.env
.env.*
*.local

# Arquivos de sistema operacional
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# (ignores específicos de stack — node_modules/, target/, dist/, etc. — entram quando a stack for definida)
